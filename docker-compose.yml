services:
  postgres:
    image: postgres:15-alpine
    container_name: ntb-database
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - ntb-network
    restart: always
    env_file: .env

  ntb-server:
    build:
      context: .
      dockerfile: Dockerfile.server
    container_name: ntb-proxy-server
    ports:
      - "8080:8080"
      - "8000:8000"
      - "9000:9000"
    depends_on:
      - postgres
    networks:
      - ntb-network
    restart: always
    env_file: .env
    volumes:
      - ./src/migrations/versions:/app/migrations/versions

  nginx:
    image: nginx:latest
    container_name: nginx-proxy
    ports:
      - "80:80"
      - "443:443"
    env_file: .env
    volumes:
      - /etc/letsencrypt:/etc/nginx/ssl:ro
      - ./nginx.conf.template:/etc/nginx/templates/nginx.conf.template:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - ntb-server
    networks:
      - ntb-network
    restart: always

volumes:
  postgres_data:

networks:
  ntb-network:
    driver: bridge
